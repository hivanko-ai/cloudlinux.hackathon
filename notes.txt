I want to make a simple program that will take a records from the email from Gemini. Gimini sends me a letters after meeting Imunify Demo with seperate recordings (first screenshot ). Each recording consists demo of each team. I need to move all recordings with demo of the teams to the specific spots on Google drive. Before the moving, this specific spot has to be created on Google Drive on the path "Imunify/Demo/year number/Imunify360 Product Demo" + current_month_number + "." + two_last_numbers_of_the_current_year_number (second screenshot). For each month the new folder has to be created.  That's why I need to have the tool (like rasing window or something like that where I can enter order in which I want to place recordings). For example we have 3 recordings (rec1, rec2, rec3) and the order is [3, 1, 2] so we will place files accordingly (rec3, rec1, rec2)
After the new folder in the acoording path will be created we have to move all recordings (in a given order) from letter from Gemini to Google drive's folder (the created one).
Then we need to rename the recording files. For this we need to open the table on Slite page (screenshot 3) and take last rows (as many as recordings we got from the email, if 3 recordings then 3 last rows) (screenshots 4 this rows are in red circle). The subsequence of recording files' names are accorded to the table on Slite. Then we have to take links (which should be automatically changed to public by the program) to the uploaded recordings from Google drive (screenshot 5 , here the path highlights made by grey color) and copypaste them to the table on Slite to column "Recording" seperatly for each recording according to what's written in column "Feature" in that row.